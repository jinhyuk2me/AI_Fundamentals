## TL;DR
- PyTorch Tensor 학습 흐름을 한눈에 정리하고 각 노트의 역할을 소개합니다.
- 모든 하위 노트는 동일한 섹션 구조(TL;DR → 언제 쓰나 → 주요 API → 실습 예제 → 실수 주의 → 관련 노트)를 따릅니다.
- “다음 이동” 링크를 따라가면 기초에서 심화까지 자연스럽게 학습할 수 있습니다.

## 학습 로드맵
| 단계  | 주제             | 핵심 질문                    | 바로 가기                       | 다음 이동                       |
| --- | -------------- | ------------------------ | --------------------------- | --------------------------- |
| 1   | Tensor 생성      | 어떤 방식으로 텐서를 만들까?         | [[Tensor 생성]]       | [[Tensor 조작]]       |
| 2   | Tensor 조작      | 모양·차원을 어떻게 맞출까?          | [[Tensor 조작]]       | [[Tensor 인덱싱]]      |
| 3   | 인덱싱 / 슬라이싱     | 필요한 데이터만 어떻게 고를까?        | [[Tensor 인덱싱]]      | [[Tensor 연산]]       |
| 4   | 연산             | 값을 어떻게 계산·비교할까?          | [[Tensor 연산]]       | [[Tensor 변환]]       |
| 5   | dtype / device | 텐서를 원하는 타입·장치로 옮길 수 있을까? | [[Tensor 변환]]       | [[Tensor 가속]]       |
| 6   | 입출력            | 텐서와 체크포인트를 어떻게 저장·불러올까?  | [[Tensor 입출력]]      | [[Tensor Autograd]] |
| 7   | Autograd       | Gradient는 어떻게 추적·차단할까?   | [[Tensor Autograd]] | [[Tensor 가속]]       |
| 8   | 성능 가속          | GPU·AMP를 어떻게 활용할까?       | [[Tensor 가속]]       | -                           |

## 노트 템플릿 안내
- `## TL;DR` : 핵심 메시지 2~3줄로 파악
- `## 언제 쓰나` : 대표 사용 시나리오 정리
- `## 주요 API` : 카테고리별 표와 기억 포인트
- `## 실습 예제` : 바로 실행 가능한 코드 스니펫
- `## 실수 주의` : 자주 틀리는 포인트와 해결책
- `## 관련 노트` : 인접 주제 링크

## 빠른 참고
- 예제에서는 `import torch`와 `torch.manual_seed(42)` 같은 기본 설정을 반복 표기하지 않습니다.
- dtype과 device 표기는 `float32 @ cuda:0` 형식으로 통일합니다.

## 다음으로
- PyTorch 전체 로드맵은 `[[PyTorch Overview]]` 노트에서 확인하세요.
- 모델 구성 심화는 `[[Modeling/Module Basics]]`부터 시작해 세부 주제로 이어집니다.
